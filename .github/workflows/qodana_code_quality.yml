# Qodana code quality workflow — temporarily disabled
# To re-enable: uncomment the full workflow and add push/pull_request triggers

name: Qodana

on:
  workflow_dispatch:

jobs:
  qodana:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Qodana is currently disabled"
